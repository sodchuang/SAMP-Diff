# Install after creating the conda env and installing torch.
# Use: pip install -r requirements_train_h100.txt
# Do not install this file with --no-deps: torchcfm / torchdyn need their
# normal Python dependency tree. Only the legacy MuJoCo / robosuite simulator
# stack should be isolated.
torchcfm==1.0.7
torchdyn==1.0.6
torch-dct==0.1.6
